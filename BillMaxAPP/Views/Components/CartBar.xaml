<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="BillMaxAPP.Views.Components.CartBar"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Border
        BackgroundColor="#16A34A"
        Padding="16,12"
        StrokeThickness="0">

        <Border.StrokeShape>
            <RoundRectangle CornerRadius="14" />
        </Border.StrokeShape>

        <Grid ColumnDefinitions="*,Auto">

            <VerticalStackLayout Grid.Column="0" Spacing="2">
                <Label
                    Text="{Binding ItemCount, Source={x:Reference Root}, StringFormat='{0} Items Added'}"
                    TextColor="White"
                    FontSize="12" />
                <Label
                    Text="{Binding CartTotal, Source={x:Reference Root}, StringFormat='₹{0:F2}'}"
                    TextColor="White"
                    FontAttributes="Bold"
                    FontSize="17" />
            </VerticalStackLayout>

            <HorizontalStackLayout Grid.Column="1" Spacing="6" VerticalOptions="Center">
                <Label Text="View Cart" TextColor="White" FontAttributes="Bold" VerticalOptions="Center" />
                <Label Text="&#8250;" TextColor="White" FontSize="20" VerticalOptions="Center" />
            </HorizontalStackLayout>

        </Grid>

        <Border.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding TapCommand, Source={x:Reference Root}}" />
        </Border.GestureRecognizers>

    </Border>

</ContentView>
